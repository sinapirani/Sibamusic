

const SongText = ({props}) => {
    return (
      <div>
        <h1 className={` text-[#00A2B8] text-6xl lg:text-8xl font-extrabold mb-7 `}>
          Add URL |<br />
          Select <span className="text-white">Song</span>
        </h1>
      </div>
    );
}

export default SongText;